<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Password Recovery</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            color: #333;
            padding: 20px;
        }

        .container {
            background-color: #fff;
            padding: 20px;
            margin: 0;
            border-radius: 0;
            box-shadow: none;
            border: 1px solid #e0e0e0;
            box-sizing: border-box;
            width: 100%;
            max-width: 600px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .logo-bayarea {
            height: 120px;
            width: auto;
            margin: 0 5px;
            vertical-align: middle;
        }

        .content {
            font-size: 16px;
            line-height: 1.6;
        }

        .code {
            background-color: #e8f0fe;
            color: #1a73e8;
            font-weight: bold;
            font-size: 24px;
            letter-spacing: 3px;
            padding: 15px 15px;
            text-align: center;
            margin: 20px auto;
            border-radius: 10px;
            border: 2px dashed #a7c7ed;
            max-width: 350px;
            word-break: break-all;
            font-family: 'Courier New', monospace;
        }

        .footer {
            text-align: center;
            font-size: 12px;
            color: #888;
            margin-top: 30px;
        }

        @media only screen and (min-width: 601px) {
            .container {
                padding: 30px;
                margin: 20px auto;
                border-radius: 12px;
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
            }
            .header img {
                margin: 0 10px;
            }
            .logo-bayarea {
                height: 220px;
            }
            .code {
                font-size: 28px;
                padding: 20px 25px;
                margin: 30px auto;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <img src="{{BayareaLogoUrl}}" alt="Bay Area Logo" class="logo-bayarea" />
        </div>
        <div class="content">
            <p>Hello,</p>
            <p>You requested to recover your password. Please use the code below to validate the change:</p>
            <div class="code">{{code}}</div>
            <p><strong>Attention:</strong> This code is temporary and will expire in a few minutes. If you did not make this request, please ignore this email.</p>
            <p>Best regards,<br><strong>Bay Area | IESB Team</strong></p>
        </div>
        <div class="footer">
            © 2025 Bay Area / IESB - All rights reserved.
        </div>
    </div>
</body>

</html>
